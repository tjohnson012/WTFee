{
  "name": "wtfee-backend",
  "version": "1.0.0",
  "description": "WTFee AWS Lambda backend for medical bill processing",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/local-server.ts",
    "deploy": "npm run build && cd infrastructure && cdk deploy",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.450.0",
    "@aws-sdk/client-textract": "^3.450.0",
    "@aws-sdk/s3-request-presigner": "^3.450.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.125",
    "@types/node": "^20.9.0",
    "@types/uuid": "^9.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.2.2"
  }
}
